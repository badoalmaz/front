import React from "react";
import PlacesList from "../components/Places/PlacesList";

const PlacesPage = () => {
  return (
    <div>
      <PlacesList />
    </div>
  );
};

export default PlacesPage;
